import { StyleSheet } from "react-native";
import { _COLORS, FONTFAMILY } from "../../../Themes";
export const RowTextCss = StyleSheet.create({
  TextView: { flex: 1, flexDirection: "row", marginTop: 10 },
  leftText: { flex: 1, alignSelf: "center" },
});

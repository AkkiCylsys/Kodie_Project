import { StyleSheet } from "react-native";
import { _COLORS, FONTFAMILY } from "../../../Themes";
export const BudgetCss = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "row",
    marginTop: 15,
  },
  textStyle: { color: _COLORS.Kodie_GrayColor },
});

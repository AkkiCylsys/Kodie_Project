import { StyleSheet } from "react-native";
import { FONTFAMILY, _COLORS } from "../../../Themes/index";
export const SearchPlacesStyle = StyleSheet.create({
  container: {
    flex:1
    // position: "absolute",
    // top: -350, // Adjust the top position as needed
    // width: "100%",
    // zIndex: 1, // Ensure the search box appears above the map
  },
});

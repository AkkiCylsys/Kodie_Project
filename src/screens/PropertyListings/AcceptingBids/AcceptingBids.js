import { View, Text } from 'react-native'
import React from 'react'
import { _COLORS } from '../../../Themes'

const AcceptingBids = () => {
  return (
    <View style={{flex:1}}>
      <Text style={{color:_COLORS.Kodie_BlackColor}}>AcceptingBids</Text>
      <Text>AcceptingBids</Text>
      <Text>AcceptingBids</Text>
      <Text>AcceptingBids</Text>
    </View>
  )
}

export default AcceptingBids